# Copy this file to .env and fill in your own values
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/fraudguard_db?retryWrites=true&w=majority
PORT=4000

# Shruti's FastAPI service (agent-service/main.py) - implements both
# /predict and /investigate per docs/api-contract.md.
# Run it first: cd agent-service && python run_service.py
AGENT_SERVICE_URL=http://127.0.0.1:8001
